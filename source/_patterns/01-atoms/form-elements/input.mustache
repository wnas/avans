<input
  class="input {{# type }}input--{{ type }}{{/ type }} {{ input-class }}"
  type="{{ type }}"
  {{# id }} id="{{ id }}" {{/ id }}
  {{# attributes }} {{ name }}="{{ value }}" {{/ attributes }}
   />
